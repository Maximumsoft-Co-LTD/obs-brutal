package svc

import (
	"context"

	"obs-brutal/boeng"
)

func PublishEvent(ctx context.Context, ev Event) {
	_ = boeng.Run(ctx, "publish_event", ev, func(ctx context.Context) error {
		bus.Publish(ctx, ev)
		return nil
	})
}
